info:
  name: openid-configuration
  type: http
  seq: 1

http:
  method: GET
  url: "{{realmUrl}}/.well-known/openid-configuration"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
