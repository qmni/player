info:
  name: Löschen
  type: folder
  seq: 5

request:
  auth:
    type: oauth2
    flow: resource_owner_password_credentials
    accessTokenUrl: "{{oidcTokenUrl}}"
    credentials:
      clientId: "{{clientId}}"
      clientSecret: 0oSEd7oLFb3NBUywGhPmyLksV29RWCfS
      placement: body
    resourceOwner:
      username: admin
      password: p
    tokenConfig:
      id: credentials
      placement:
        header: Bearer
      source: access_token
    settings:
      autoFetchToken: true
      autoRefreshToken: false
